# rag_service requirements
# Core retrieval stack
pinecone>=6.0.0
sentence-transformers>=2.6.0

# LLM HTTP client (provider-agnostic OpenAI-compat)
httpx>=0.27.0

# Pydantic validation
pydantic>=2.0.0

# YAML policy files
pyyaml>=6.0

# Environment variable loading
python-dotenv>=1.0.0

# Benchmark progress bar
tqdm>=4.66.0

# Fuzzy string matching (used by IngredientResolver / text_match)
rapidfuzz>=3.0.0

# Optional: RabbitMQ transport (comment out if unused)
pika>=1.3.0

# Optional: FastAPI wrapper (comment out if unused)
# fastapi>=0.110.0
# uvicorn[standard]>=0.29.0
